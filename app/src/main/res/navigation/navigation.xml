<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/initFragment">

    <fragment
        android:id="@+id/initFragment"
        android:name="com.androidandrew.sunscreen.ui.init.InitFragment"
        tools:layout="@layout/fragment_init" >
        <action
            android:id="@+id/action_initFragment_to_locationFragment"
            app:destination="@id/locationFragment"
            app:popUpTo="@id/initFragment"
            app:popUpToInclusive="true"/>
        <action
            android:id="@+id/action_initFragment_to_mainFragment"
            app:destination="@id/mainFragment"
            app:popUpTo="@id/initFragment"
            app:popUpToInclusive="true"/>
    </fragment>

    <fragment
        android:id="@+id/locationFragment"
        android:name="com.androidandrew.sunscreen.ui.location.LocationFragment"
        tools:layout="@layout/fragment_location" >
        <action
            android:id="@+id/action_locationFragment_to_mainFragment"
            app:destination="@id/mainFragment"
            app:popUpTo="@id/locationFragment"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/mainFragment"
        android:name="com.androidandrew.sunscreen.ui.main.MainFragment"
        android:label="@string/app_name"
        tools:layout="@layout/fragment_main" />

</navigation>